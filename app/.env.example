# Copy this file to .env.local and fill in your real key.
# Used by the Vite dev proxy (vite.config.js) so the browser can call /api/anthropic
# without exposing the key in client code.
ANTHROPIC_API_KEY=sk-ant-...
